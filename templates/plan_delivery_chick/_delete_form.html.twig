<form method="post" action="{{ path('plan_delivery_chick_delete', {'id': plan_delivery_chick.id}) }}"
      onsubmit="return confirm('{% trans %}plan_delivery_chick.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plan_delivery_chick.id) }}">
    {{ include('layout/buttons/delete.html.twig', { text: 'plan_delivery_chick.delete.button.delete' }) }}
</form>
