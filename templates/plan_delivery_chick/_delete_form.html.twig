<form method="post" action="{{ path('plan_delivery_chick_delete', {'id': plan_delivery_chick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plan_delivery_chick.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
