{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <div class="d-flex justify-content-center card-group">
        {% include 'layout/cards/chead.html.twig' %}
        {% trans %}eggs_inputs_transfer.edit.title{% endtrans %}
        {% include 'layout/cards/cbody.html.twig' %}
        {{ include('eggs_inputs_transfers/_form.html.twig', {'button_label': 'eggs_inputs_transfer.edit.button.update'}) }}
        {% include 'layout/cards/cfooter.html.twig' %}
        {{ include('eggs_inputs_transfers/_delete_form.html.twig') }}
        {% include 'layout/cards/cend.html.twig' %}
    </div>
    {{ include('layout/buttons/back.html.twig',
        { pathLink: path('eggs_inputs_transfers_index'), text: 'eggs_inputs_transfer.edit.button.back' }) }}

{% endblock %}
