<form method="post" action="{{ path('eggs_inputs_transfers_delete', {'id': eggs_inputs_transfer.id}) }}"
      onsubmit="return confirm('{% trans %}eggs_inputs_transfer.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eggs_inputs_transfer.id) }}">
    <button class="btn btn-danger">
        {% trans %}eggs_inputs_transfer.delete.button.delete{% endtrans %}
    </button>
</form>
