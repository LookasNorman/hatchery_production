{% extends 'base.html.twig' %}

{% block title %}InputsFarmDelivery{% endblock %}

{% block body %}
    <h1>InputsFarmDelivery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inputs_farm_delivery.id }}</td>
            </tr>
            <tr>
                <th>EggsNumber</th>
                <td>{{ inputs_farm_delivery.eggsNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inputs_farm_delivery_index') }}">back to list</a>

    <a href="{{ path('inputs_farm_delivery_edit', {'id': inputs_farm_delivery.id}) }}">edit</a>

    {{ include('inputs_farm_delivery/_delete_form.html.twig') }}
{% endblock %}
