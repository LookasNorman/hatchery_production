<form method="post" action="{{ path('customer_phone_delete', {'id': customer_phone.id}) }}"
      onsubmit="return confirm('{% trans %}customer_phone.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_phone.id) }}">
    {{ include('layout/buttons/delete.html.twig', { text: 'customer_phone.delete.button.delete' }) }}
</form>
