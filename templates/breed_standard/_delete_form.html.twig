<form method="post" action="{{ path('breed_standard_delete', {'id': breed_standard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ breed_standard.id) }}">
    <button class="btn">Delete</button>
</form>
