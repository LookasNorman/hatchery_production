{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <div class="d-flex justify-content-center card-group">
        <div class="card">
            <h5 class="card-header">
                {% trans %}hatchers.new.title{% endtrans %}
            </h5>
            <div class="card-body">
                {{ include('hatchers/_form.html.twig') }}
            </div>
        </div>
    </div>
    <a class="btn btn-primary" href="{{ path('hatchers_index') }}">
        {% trans %}hatchers.new.button.back{% endtrans %}
    </a>
{% endblock %}
