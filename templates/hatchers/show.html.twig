{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <h1>{% trans %}hatchers.show.title{% endtrans %}</h1>
    <div class="d-flex justify-content-center card-group">
        <div class="card">
            <h5 class="card-header">
                {{ hatcher.name }}
            </h5>
            <div class="card-body">
                <p class="card-text">
                    {% trans %}hatchers.show.shortname{% endtrans %}
                    : {{ hatcher.shortname }}
                </p>
            </div>
            <div class="card-footer bg-transparent border-success d-flex">
                {% if is_granted('ROLE_ADMIN') %}
                    {{ include('hatchers/_delete_form.html.twig') }}
                    <a class="btn btn-warning" href="{{ path('hatchers_edit', {'id': hatcher.id}) }}">
                        {% trans %}hatchers.show.button.edit{% endtrans %}
                    </a>
                {% endif %}
            </div>
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('hatchers_index') }}">
        {% trans %}hatchers.show.button.back{% endtrans %}
    </a>
{% endblock %}
