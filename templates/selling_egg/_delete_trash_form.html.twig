<form method="post" action="{{ path('selling_egg_delete', {'id': selling_egg.id}) }}"
      onsubmit="return confirm('{% trans %}selling_egg.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ selling_egg.id) }}">
    <button class="btn"><i class="fa fa-trash"></i></button>
</form>
