<form method="post" action="{{ path('car_delete', {'id': car.id}) }}" onsubmit="return confirm('{% trans %}car.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
    {{ include('layout/buttons/delete.html.twig', { text: 'car.delete.button.delete' }) }}
</form>
