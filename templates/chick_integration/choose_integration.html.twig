{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 1920px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>{% trans %}chick_integration.choose_integration.title{% endtrans %}</h1>
        <div class="d-flex justify-content-center flex-wrap">
            <div class="d-flex justify-content-center card-group">
                {% include 'layout/cards/chead.html.twig' %}
                <i class="fa fa-plus"></i>
                {% trans %}chick_integration.choose_integration.header{% endtrans %}
                {% include 'layout/cards/cbody.html.twig' %}
                {{ form_start(form) }}
                {{ form_widget(form) }}
{#                {{ include('layout/buttons/create.html.twig', {text: 'chick_integration.choose_integration.form.button.save'}) }}#}
                {{ form_end(form) }}
                {% include 'layout/cards/cfooter.html.twig' %}
                {% include 'layout/cards/cend.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}
