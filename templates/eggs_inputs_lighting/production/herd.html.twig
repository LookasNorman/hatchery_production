{% extends 'base.html.twig' %}

{% block body %}
    <div class="text-center m-2">
        <h1>
            {% trans %}eggs_inputs_lighting.production.herd.title{% endtrans %}
        </h1>
    </div>
    <div class="d-flex justify-content-center flex-wrap">
        {% for herd in herds %}
            <a class="btn btn-warning p-4 m-2 col-sm-3" href="{{ path('eggs_inputs_lighting_new', {'herd': herd.id, 'inputs': inputs.id}) }}">
                <i class="far fa-lightbulb fa-2x"></i> {{ herd.name }}
            </a>
        {% endfor %}
    </div>
{% endblock %}
