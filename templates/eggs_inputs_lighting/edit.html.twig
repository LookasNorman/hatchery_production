{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <div class="d-flex justify-content-center card-group">
        {% include 'layout/cards/chead.html.twig' %}
        {% trans %}eggs_inputs_lighting.edit.title{% endtrans %}
        {% include 'layout/cards/cbody.html.twig' %}
        {{ include('eggs_inputs_lighting/_form.html.twig', {'button_label': 'eggs_inputs_lighting.edit.button.update'}) }}
        {% include 'layout/cards/cfooter.html.twig' %}
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('eggs_inputs_lighting/_delete_form.html.twig') }}
        {% endif %}
        {% include 'layout/cards/cend.html.twig' %}
    </div>
    {{ include('layout/buttons/back.html.twig',
        { pathLink: path('eggs_inputs_lighting_index'), text: 'eggs_inputs_lighting.edit.button.back' }) }}

{% endblock %}
