{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-flex justify-content-center card-group">
        {% include 'layout/cards/chead.html.twig' %}
        {% trans %}eggs_inputs_selections.edit.title{% endtrans %}
        {% include 'layout/cards/cbody.html.twig' %}
        {{ include('eggs_selections/_form.html.twig', {'button_label': 'eggs_inputs_selections.edit.button.update'}) }}
        {% include 'layout/cards/cfooter.html.twig' %}
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('eggs_selections/_delete_form.html.twig') }}
        {% endif %}
        {% include 'layout/cards/cend.html.twig' %}
    </div>
    {{ include('layout/buttons/back.html.twig',
        { pathLink: path('eggs_selections_index'), text: 'eggs_inputs_selections.edit.button.back' }) }}

{% endblock %}
