<form method="post" action="{{ path('eggs_selections_delete', {'id': eggs_selection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eggs_selection.id) }}">
    <button class="btn">Delete</button>
</form>
