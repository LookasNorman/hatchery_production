{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <div class="d-flex justify-content-center card-group">
        <div class="card">
            <h5 class="card-header">
                {% trans %}eggs_delivery.edit.title{% endtrans %}
            </h5>
            <div class="card-body">
                {{ include('eggs_delivery/_form.html.twig', {'button_label': 'eggs_delivery.edit.button.update'}) }}
            </div>
        </div>
    </div>
    <a class="btn btn-primary" href="{{ path('eggs_delivery_index') }}">
        {% trans %}eggs_delivery.edit.button.back{% endtrans %}
    </a>
    {{ include('eggs_delivery/_delete_form.html.twig') }}
{% endblock %}
