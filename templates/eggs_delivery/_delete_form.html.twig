<form method="post" action="{{ path('eggs_delivery_delete', {'id': eggs_delivery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eggs_delivery.id) }}">
    <button class="btn">Delete</button>
</form>
