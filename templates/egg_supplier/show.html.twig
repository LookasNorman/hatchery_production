{% extends 'base.html.twig' %}

{% block title %}EggSupplier{% endblock %}

{% block body %}
    <h1>EggSupplier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ egg_supplier.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ egg_supplier.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ egg_supplier.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ egg_supplier.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('egg_supplier_index') }}">back to list</a>

    <a href="{{ path('egg_supplier_edit', {'id': egg_supplier.id}) }}">edit</a>

    {{ include('egg_supplier/_delete_form.html.twig') }}
{% endblock %}
