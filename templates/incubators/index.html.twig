{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>
            {% trans %}incubators.index.title{% endtrans %}
        </h1>
    </div>
    <div class="d-flex card-group justify-content-center">
        {% include('incubators/setters.html.twig') %}
        {% include('incubators/hatchers.html.twig') %}
    </div>
{% endblock %}
