{% extends 'base.html.twig' %}

{% block title %}PlanDeliveryEgg{% endblock %}

{% block body %}
    <h1>PlanDeliveryEgg</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan_delivery_egg.id }}</td>
            </tr>
            <tr>
                <th>EggsNumber</th>
                <td>{{ plan_delivery_egg.eggsNumber }}</td>
            </tr>
            <tr>
                <th>DeliveryDate</th>
                <td>{{ plan_delivery_egg.deliveryDate ? plan_delivery_egg.deliveryDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plan_delivery_egg_index') }}">back to list</a>

    <a href="{{ path('plan_delivery_egg_edit', {'id': plan_delivery_egg.id}) }}">edit</a>

    {{ include('plan_delivery_egg/_delete_form.html.twig') }}
{% endblock %}
