<form method="post" action="{{ path('plan_input_farm_delete', {'id': planFarm.id}) }}"
      onsubmit="return confirm('{% trans %}plan_input_farm.delete.confirm{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planFarm.id) }}">
    {{ include('layout/buttons/delete.html.twig', { text: 'plan_input_farm.delete.button.delete' }) }}
</form>
