{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 380px;
            padding: 6px 6px;
        }
    </style>
    <div class="d-flex justify-content-center card-group">
        <div class="card">
            <h5 class="card-header">
                {% trans %}breed.edit.title{% endtrans %}
            </h5>
            <div class="card-body">
                {{ include('breed/_form.html.twig', {'button_label': 'breed.edit.button.update'}) }}
            </div>
        </div>
    </div>
    <a class="btn btn-primary" href="{{ path('breed_index') }}">
        {% trans %}breed.edit.button.back{% endtrans %}
    </a>
    {{ include('breed/_delete_form.html.twig') }}
{% endblock %}
