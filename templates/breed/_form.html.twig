{{ form_start(form) }}
<div class="form-group">
    <label>{{ form_label(form.name) }}</label>
    {{ form_widget(form.name) }}
    <small>{{ form_help(form.name) }}</small>
    <div class="form-text text-danger">
        {{ form_errors(form.name) }}
    </div>
</div>
<div class="form-group">
    <label>{{ form_label(form.type) }}</label>
    {{ form_widget(form.type) }}
    <small>{{ form_help(form.type) }}</small>
    <div class="form-text text-danger">
        {{ form_errors(form.type) }}
    </div>
</div>
<div class="form-group">
    <label>{{ form_label(form.lighting) }}</label>
    {{ form_widget(form.lighting) }}
    <small>{{ form_help(form.lighting) }}</small>
    <div class="form-text text-danger">
        {{ form_errors(form.lighting) }}
    </div>
</div>
{{ include('layout/buttons/create.html.twig', {text: 'breed.form.button.save'}) }}
{{ form_end(form) }}
