{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>
            {% trans %}main_page.index.title{% endtrans %}
        </h1>
    </div>
    <div class="d-flex card-group justify-content-center">
        {% include('main_page/supplier.html.twig') %}
        {% include('main_page/recipient.html.twig') %}
        {% include('main_page/inputs.html.twig') %}
    </div>
    <div class="d-flex card-group justify-content-center">
    {% include('main_page/eggs_transfer.html.twig') %}
    {% include('main_page/picking.html.twig') %}
    </div>
{% endblock %}
