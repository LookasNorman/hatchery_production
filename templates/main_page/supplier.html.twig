{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 400px;
            padding: 6px 6px;
        }
    </style>
    <div class="card ">
        <h4 class="card-header">
            {% trans %}main_page.supplier.title{% endtrans %}
        </h4>
        <div class="card-horizontal">
            <div class="card-body">
                <table class="table table-hover">
                    <tbody>
                    <tr>
                        <th scope="row">{% trans %}main_page.supplier.body.supplier_number{% endtrans %}</th>
                        <td>{{ suppliers.suppliersNumber }}</td>
                    </tr>
                    <tr>
                        <th scope="row">{% trans %}main_page.supplier.body.eggs_number{% endtrans %}</th>
                        <td>{{ suppliers.eggsInWarehouse|number_format(0, ',', ' ') }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            {{ include('layout/buttons/back.html.twig',
                { pathLink: path('egg_supplier_index'), text: 'main_page.supplier.list'}) }}
            {% if is_granted('ROLE_ADMIN') %}
                {{ include('layout/buttons/create.html.twig',
                    { pathLink: path('egg_supplier_new'), text: 'main_page.supplier.new' }) }}
            {% endif %}
        </div>
    </div>
{% endblock %}
