{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 400px;
            padding: 6px 6px;
        }
    </style>
    <div class="card ">
        <h4 class="card-header">
            {% trans %}main_page.eggs_transfer.title{% endtrans %}
        </h4>
        <div class="card-horizontal">
            <div class="card-body">
                <table class="table table-hover">
                    <thead class="thead-light">
                    <tr>
                        <th scope="col">
                            {% trans %}main_page.eggs_transfer.table.inputs_name{% endtrans %}
                        </th>
                        <th scope="col">
                            {% trans %}main_page.eggs_transfer.table.date{% endtrans %}
                        </th>
                        <th scope="col">
                            {% trans %}main_page.eggs_transfer.table.eggs_number{% endtrans %}
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for transfer in transfers %}
                        <tr>
                            <th scope="row">
                                <a href="{{ path('eggs_inputs_show', {'id': transfer.0.id}) }}">{{ transfer.0.name }}</a>
                            </th>
                            <td>{{ transfer.0.inputDate ? transfer.0.inputDate|date_modify("+18 day")|date('Y-m-d') : '' }}</td>
                            <td>{{ (transfer.eggs - transfer.waste)|number_format(0, ',', ' ') }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            {{ include('layout/buttons/back.html.twig',
                { pathLink: path('no_transfer_index'), text: 'main_page.eggs_transfer.list'}) }}
        </div>
    </div>
{% endblock %}
