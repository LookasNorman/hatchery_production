{% block body %}
    <style>
        .card {
            max-width: 500px;
            min-width: 400px;
            padding: 6px 6px;
        }
    </style>
    <div class="card ">
        <h4 class="card-header">
            {% trans %}main_page.lighting.title{% endtrans %}
        </h4>
        <div class="card-horizontal">
            <div class="card-body">
                <table class="table table-hover">
                    <thead class="thead-light">
                    <tr>
                        <th scope="col">
                            {% trans %}main_page.lighting.table.inputs_name{% endtrans %}
                        </th>
                        <th scope="col">
                            {% trans %}main_page.lighting.table.date{% endtrans %}
                        </th>
                        <th scope="col">
                            {% trans %}main_page.lighting.table.eggs_number{% endtrans %}
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for lighting in lightings %}
                        <tr>
                            <th scope="row">
                                <a href="{{ path('eggs_inputs_show', {'id': lighting.0.id}) }}">{{ lighting.0.name }}</a>
                            </th>
                            <td>{{ lighting.0.inputDate ? lighting.0.inputDate|date_modify("+12 day")|date('Y-m-d') : '' }}</td>
                            <td>{{ lighting.eggs|number_format(0, '', ' ') }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            {{ include('layout/buttons/back.html.twig',
                { pathLink: path('no_lighting_index'), text: 'main_page.lighting.list'}) }}
        </div>
    </div>
{% endblock %}
