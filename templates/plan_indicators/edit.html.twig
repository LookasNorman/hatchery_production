{% extends 'base.html.twig' %}

{% block title %}Edit PlanIndicators{% endblock %}

{% block body %}
    <h1>Edit PlanIndicators</h1>

    {{ include('plan_indicators/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('plan_indicators_index') }}">back to list</a>
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('plan_indicators/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
